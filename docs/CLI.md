# BXZ CLI

`bxz` starts the REPL.

`bxz -v` / `bxz --version` shows the official version.

`bxz -h` / `bxz --help` shows help.

`bxz -a` / `bxz --about` shows information.

`bxz -s` / `bxz --server` starts the server.

`bxz -p <port>` selects a port.

`bxz file.bxz` runs a source file.
